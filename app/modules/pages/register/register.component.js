// Register the `register` component on the `register` module,
angular.module('register').component('register', {
  templateUrl: 'modules/pages/register/register.template.html',
  controller: function (UserService, $location) {
  }
});
